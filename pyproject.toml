[project]
name = "paraketto"
version = "0.1.0"
description = "Local voice dictation using Parakeet V2 — native CUDA/C++ inference"
requires-python = ">=3.10"
dependencies = [
    "soundfile",
    "numpy",
    "jiwer",
    "requests",
    "onnx",
    "datasets>=4.6.1",
    "torchcodec>=0.10.0",
]

