[project]
name = "parakeet"
version = "0.1.0"
description = "Local voice dictation using Parakeet V2 via onnx-asr"
requires-python = ">=3.10"
dependencies = [
    "onnx-asr[gpu,hub]",
    "soundfile",
    "numpy",
    "tensorrt-cu12",
    "jiwer",
    "requests",
]

